# 그룹 모임 관리 시스템 기능 명세서

## 1. 시스템 개요

본 프로젝트는 Java Swing GUI를 통해 여러 사용자가 모임방을 만들고, 일정·장소·참여 인원·비용을 함께 관리하는 시스템이다.  
모임은 **단체 약속**과 **단체 여행**으로 구분되며, 모든 정보는 프로젝트의 CSV 파일에 저장된다.

### 사용자 구분
- **일반 참여자**: 회원가입과 로그인, 모임 참여·탈퇴, 가능한 모임 일정 제출, 원하는 모임 장소 투표, 모임 회차 참여, 비용 확인 및 본인 정산 완료 처리를 할 수 있다.
- **방장**: 일반 참여자 기능에 더해 참여자 강퇴, 모임 회차 생성, 추천 일정 확정, 최종 날짜·시간·장소 수정을 할 수 있다.

---

## 2. 전체 이용 흐름

1. 프로그램 실행
2. 회원가입 또는 로그인
3. 참여 중인 모임방 목록 확인
4. 모임방 생성 또는 4자리 코드로 기존 방 참여
5. 모임 종류에 따라 일정 입력
   - 단체 약속: 가능한 날짜와 시간 입력
   - 단체 여행: 가능한 여행 시작일과 종료일 입력
6. 모임방 상세 화면에서 일정·장소·회차·비용 관리
7. 방장이 최종 날짜·시간·장소 확정
8. 지출 입력 후 정산 계산 및 입금 완료 확인

---

## 3. 회원 기능

### 3.1 회원가입

사용자가 로그인 화면에서 **회원가입**을 선택하면 다음 정보를 입력한다.

- 이름
- 닉네임
- 로그인 아이디
- 비밀번호
- 비밀번호 확인
- 이메일

시스템은 다음 순서로 입력값을 검증한다.

1. 모든 필수 항목이 입력되었는지 확인한다.
2. 비밀번호가 4자 이상인지 확인한다.
3. 비밀번호와 비밀번호 확인 값이 일치하는지 확인한다.
4. 이메일이 `사용자명@도메인.확장자` 형태인지 확인한다.
5. 동일한 로그인 아이디가 이미 등록되어 있는지 확인한다.
6. 검증을 통과하면 회원 정보를 저장한다.
7. 가입 완료 후 로그인 화면의 아이디 입력란에 새 아이디를 자동으로 입력하고 비밀번호 입력란으로 포커스를 이동한다.

### 3.2 로그인

사용자가 아이디와 비밀번호를 입력하면 다음과 같이 처리한다.

1. 아이디와 비밀번호가 모두 입력되었는지 확인한다.
2. 저장된 회원 정보에서 아이디와 비밀번호가 모두 일치하는 사용자를 찾는다.
3. 일치하지 않으면 로그인 실패 메시지를 표시하고 비밀번호 입력란을 비운다.
4. 일치하면 환영 메시지를 표시하고 메인 화면으로 이동한다.

### 3.3 회원 정보 수정

사용자는 메인 화면 또는 모임방 화면에서 **마이페이지**를 열어 다음 정보를 수정할 수 있다.

- 이름
- 닉네임
- 이메일
- 비밀번호

적용 규칙은 다음과 같다.

- 로그인 아이디는 조회만 가능하며 변경할 수 없다.
- 이름, 닉네임, 이메일은 비워 둘 수 없다.
- 이메일 형식을 다시 검증한다.
- 새 비밀번호 입력란을 비워 두면 기존 비밀번호를 유지한다.
- 비밀번호를 변경할 경우 4자 이상이어야 하며 확인 값과 일치해야 한다.
- 저장이 완료되면 현재 로그인 사용자 객체와 회원 CSV에 즉시 반영된다.

---

## 4. 메인 화면 및 모임방 관리

### 4.1 참여 중인 모임방 목록

로그인 후 현재 사용자가 참여 중인 모임방만 목록으로 표시한다. 각 항목에는 다음 정보가 나타난다.

- 모임방 이름
- 현재 참여 인원 수
- 모임 종류: 단체 약속 또는 단체 여행
- 현재 실행 중 새로 만든 방인 경우 신규 표시
- 방 나가기 버튼

모임방 항목을 선택하면 해당 방의 상세 화면으로 이동한다.

### 4.2 모임방 검색

사용자가 검색어를 입력하면 본인이 참여 중인 방 가운데 **방 이름에 검색어가 포함된 방**만 표시한다.

- 검색어가 없으면 참여 중인 모든 방을 표시한다.
- 결과가 없으면 검색 결과가 없다는 안내를 표시한다.

### 4.3 모임방 생성

사용자가 **방 만들기**를 선택하면 다음 정보를 입력한다.

- 방 이름
- 사용자가 지정하는 4자리 숫자 방 코드
- 모임 종류
  - 단체 약속
  - 단체 여행

시스템은 다음 조건을 검사한다.

1. 방 이름이 입력되었는지 확인한다.
2. 방 코드 네 자리가 모두 입력되었는지 확인한다.
3. 방 코드가 숫자인지 확인한다.
4. 방 코드가 1000~9999 범위인지 확인한다.
5. 이미 사용 중인 코드인지 확인한다.
6. 모임 종류가 선택되었는지 확인한다.

생성이 완료되면 다음과 같이 처리한다.

- 생성자를 방장으로 지정한다.
- 생성자를 첫 번째 참여자로 자동 등록한다.
- 단체 약속이면 가능한 날짜·시간 및 추천 장소 입력 화면을 연다.
- 단체 여행이면 가능한 여행 기간 입력 화면을 연다.
- 입력 화면을 닫으면 새 모임방 상세 화면으로 이동한다.

### 4.4 모임방 참여

사용자가 **방 참여**를 선택하면 4자리 방 코드를 입력한다.

1. 코드 네 자리가 모두 입력되었는지 확인한다.
2. 해당 코드의 방이 존재하는지 확인한다.
3. 사용자가 이미 참여 중인지 확인한다.
4. 조건을 통과하면 방의 참여자 목록에 사용자를 추가한다.
5. 모임 종류에 맞는 최초 일정 입력 화면을 연다.

### 4.5 모임방 나가기

사용자가 방 목록에서 **나가기**를 선택하면 확인창을 표시한다.

- 확인하면 해당 방의 참여자 목록에서 사용자를 제거한다.
- 취소하면 아무 작업도 하지 않는다.
- 처리 후 메인 화면의 방 목록을 다시 불러온다.

### 4.6 참여자 목록 및 강퇴

모임방 상세 화면에서 참여자 목록을 조회할 수 있다.

- 참여자의 닉네임을 표시한다.
- 현재 사용자가 방장인 경우 다른 참여자에게 강퇴 버튼을 표시한다.
- 방장은 자기 자신을 강퇴할 수 없다.
- 강퇴 전 확인창을 표시하며, 확인하면 방 참여자 목록에서 해당 회원을 제거한다.

---

## 5. 모임방 상세 화면

상세 화면은 모임 이름, 참여자 정보, 최종 결정 정보와 기능 메뉴를 한 화면에 제공한다.

### 공통 표시 정보

- 모임방 이름
- 모임 종류
- 참여자 목록 진입 기능
- 최종 날짜
- 최종 시작·종료 시간
- 최종 장소
- 마이페이지
- 메인 화면으로 돌아가기

### 권한 적용

- 방장만 최종 날짜·시간·장소를 직접 수정할 수 있다.
- 일정 추천 결과에서 최종 후보를 선택하는 기능도 방장에게만 제공된다.
- 단체 약속과 단체 여행에 맞지 않는 기능은 구분하여 제공된다.

---

## 6. 단체 약속 일정 조율

### 6.1 최초 일정 제출

단체 약속 방을 생성하거나 처음 참여하면 일정 입력 화면이 열린다.

사용자는 다음 정보를 입력한다.

- 달력에서 날짜 선택
- 시작 시간
- 종료 시간
- 선택적으로 추천 장소 한 건 입력

처리 과정은 다음과 같다.

1. 날짜가 선택되었는지 확인한다.
2. 종료 시간이 시작 시간보다 늦은지 확인한다.
3. 유효한 날짜·시간 구간을 임시 목록에 추가한다.
4. 여러 날짜·시간 구간을 반복하여 추가할 수 있다.
5. 임시 목록의 항목을 삭제할 수 있다.
6. 제출 시 한 개 이상의 일정이 있는지 확인한다.
7. 날짜·시간 정보는 `AvailabilityEntry`로 저장한다.
8. 추천 장소는 일정 모델에 넣지 않고 장소 후보 저장소에 별도로 저장한다.
9. 같은 이름의 장소가 이미 있으면 중복 추가하지 않는다.

### 6.2 내 일정 수정

사용자는 자신이 제출한 일정만 다시 불러와 수정할 수 있다.

- 기존 날짜·시간 목록 조회
- 새 일정 추가
- 기존 일정 선택 후 날짜·시간 수정
- 일정 삭제
- 최종 저장

저장 시 해당 방과 해당 사용자의 이전 일정만 제거하고 새 목록으로 교체한다. 다른 방이나 다른 사용자의 일정은 유지한다.

### 6.3 전체 참여자 일정 조회

참여자별로 제출한 날짜와 시간 목록을 카드 형태로 확인할 수 있다.

- 회원 아이디를 닉네임으로 변환하여 표시한다.
- 일정이 없는 참여자는 일정 없음으로 구분할 수 있다.

### 6.4 공통 일정 계산

시스템은 제출된 일정에서 참여자들이 함께 가능한 날짜와 시간 구간을 계산한다.

1. 방에 제출된 모든 일정 데이터를 불러온다.
2. 한 번도 일정을 제출하지 않은 회원은 계산에서 제외한다.
3. 동일한 날짜의 시간 구간을 비교한다.
4. 해당 날짜를 입력한 모든 대상 회원의 시간이 겹치는 구간을 계산한다.
5. 겹치는 구간을 길이가 긴 순서로 정렬한다.
6. 상위 3개 후보를 순위 카드로 표시한다.
7. 공통 시간이 없으면 아직 겹치는 시간이 없다는 안내를 표시한다.

### 6.5 추천 일정 확정

- 방장은 추천 결과 카드에서 후보를 선택할 수 있다.
- 선택 전 최종 확정 여부를 다시 확인한다.
- 확인하면 해당 날짜와 시작·종료 시간을 방의 최종 결정으로 저장한다.
- 일반 참여자는 추천 결과를 조회할 수 있지만 확정할 수 없다.

### 6.6 최종 날짜·시간 직접 수정

방장은 추천 결과와 별개로 최종 정보를 직접 지정할 수 있다.

- 최종 날짜: 달력에서 한 날짜 선택
- 최종 시간: 시작 시간과 종료 시간 선택
- 종료 시간이 시작 시간보다 늦어야 저장된다.

---

## 7. 단체 여행 일정 조율

### 7.1 최초 여행 기간 제출

단체 여행 방을 생성하거나 처음 참여하면 여행 기간 입력 화면이 열린다.

- 여행 시작 날짜 선택
- 여행 종료 날짜 선택
- 여러 개의 가능한 기간 추가
- 추가한 기간 삭제
- 전체 목록 제출

종료 날짜는 시작 날짜보다 빠를 수 없다.

### 7.2 내 여행 일정 수정

사용자는 본인이 제출한 여행 기간을 다시 불러와 다음 작업을 할 수 있다.

- 새 기간 추가
- 기존 기간 수정
- 기존 기간 삭제
- 전체 변경 사항 저장

저장 시 해당 방과 해당 회원의 여행 기간만 새 목록으로 교체한다.

### 7.3 공통 여행 기간 계산

1. 일정을 제출한 회원별 여행 가능 기간을 수집한다.
2. 회원별 날짜 구간의 교집합을 계산한다.
3. 모든 대상 회원에게 공통으로 가능한 기간만 남긴다.
4. 시작 날짜가 빠른 순서로 정렬한다.
5. 상위 공통 기간을 순위 카드로 표시한다.
6. 결과에는 시작일, 종료일, 숙박 수와 여행 일수를 표시한다.

### 7.4 최종 여행 날짜 확정 및 수정

- 방장은 추천 기간 카드를 선택하여 최종 여행 기간으로 확정할 수 있다.
- 확정 전 확인창을 표시한다.
- 방장은 달력에서 시작일과 종료일을 직접 선택하여 최종 날짜를 수정할 수도 있다.
- 저장 결과는 `시작일 ~ 종료일 (N박 M일)` 형태로 표시된다.

---

## 8. 장소 후보 관리 및 투표

### 8.1 장소 후보 등록

장소 후보는 일정 데이터와 분리하여 방 단위로 관리한다.

- 최초 단체 약속 일정 제출 시 추천 장소를 함께 등록할 수 있다.
- 장소 목록 화면에서 후보를 추가할 수 있다.
- 빈 장소명은 등록할 수 없다.
- 대소문자를 무시하고 같은 이름의 장소가 있으면 중복 등록하지 않는다.
- 기존 일정 CSV에 남아 있는 장소 값은 장소 목록이 비어 있을 때 이전 후보로 가져올 수 있다.

### 8.2 장소 후보 삭제

- 장소 삭제 전 확인 절차를 거친다.
- 장소를 삭제하면 해당 장소에 기록된 투표도 함께 삭제한다.

### 8.3 장소 투표

사용자는 방에 등록된 후보 중 한 장소에 투표할 수 있다.

- 사용자별로 한 방에서 한 표만 유지한다.
- 다른 장소에 다시 투표하면 기존 표를 새 장소로 변경한다.
- 현재 투표한 장소를 표시한다.
- 본인의 투표를 취소할 수 있다.

### 8.4 투표 결과

투표 결과 화면에서 다음 정보를 확인할 수 있다.

- 장소별 득표 수
- 장소별 투표자 목록
- 득표 순위
- 회원 아이디 대신 닉네임 표시

### 8.5 장소 무작위 추천

모임방 상세 화면은 두 종류의 무작위 추천을 제공한다.

- 등록된 장소 후보 중 하나를 무작위 선택
- 시스템에 정의된 국내 도시 목록 중 하나를 무작위 선택

등록된 후보가 없는 경우 후보가 없다는 안내를 표시한다.

### 8.6 최종 장소 확정

방장은 최종 장소 입력창에서 장소명을 입력해 방의 최종 장소로 저장할 수 있다.

---

## 9. 모임 회차 및 참여자 조사

단체 약속 방은 전체 모임 외에 `1차 모임`, `2차 모임`과 같은 회차를 별도로 관리할 수 있다.

### 9.1 회차 생성

- 방장에게만 회차 추가 버튼을 표시한다.
- 회차 이름을 입력하여 새 회차를 생성한다.
- 회차는 방 코드와 고유 ID로 구분하여 저장한다.

### 9.2 회차 목록 및 참여

- 방의 전체 회차 목록을 표시한다.
- 각 회차에 본인이 참여 중인지 표시한다.
- 참여하지 않은 회차에는 참여 버튼을 표시한다.
- 참여 전 확인창을 표시한다.
- 참여를 확정하면 다시 취소하거나 수정할 수 없다.

### 9.3 회차별 참여자 목록

회차 이름을 선택하면 해당 회차에 참여한 회원들의 닉네임 목록을 표시한다.

---

## 10. 비용 관리

### 10.1 전체 지출 조회

방에 등록된 모든 지출 내역과 총금액을 표시한다. 각 지출에는 다음 정보가 포함된다.

- 결제자
- 금액
- 지출 사유
- 메모
- 일반 지출 또는 연결된 회차

### 10.2 일반 지출 추가

일반 지출은 방 전체 참여자가 함께 부담하는 비용이다. 다음 값을 입력한다.

- 결제자
- 금액
- 지출 사유
- 선택적 메모

입력값을 검증한 후 고유 ID를 생성하여 저장한다.

### 10.3 회차별 지출 추가

회차별 지출은 선택한 회차의 참여자만 부담하는 비용이다.

- 먼저 모임 회차가 한 개 이상 존재해야 한다.
- 대상 회차 선택
- 결제자 선택
- 금액 입력
- 지출 사유 입력
- 선택적 메모 입력

회차가 없으면 먼저 회차를 생성하라는 안내를 표시한다.

### 10.4 지출 수정 및 삭제

- 일반 지출은 일반 지출 수정 화면에서 수정한다.
- 회차별 지출은 회차별 지출 수정 화면에서 수정한다.
- 결제자 본인에게만 수정·삭제 버튼을 표시한다.
- 삭제 전 확인창을 표시한다.

---

## 11. 정산 계산 및 확인

### 11.1 정산 대상 구분

정산 시 지출을 다음 그룹으로 나눈다.

- **일반 지출**: 현재 방의 전체 참여자가 균등 부담
- **회차별 지출**: 해당 회차에 참여 확정한 회원만 균등 부담

### 11.2 정산 계산 방식

1. 그룹별 전체 지출액을 계산한다.
2. 전체 금액을 참여자 수로 나누어 1인당 부담액을 계산한다.
3. 나머지가 생기면 참여자 목록 앞 순서부터 1원씩 추가한다.
4. 회원별 실제 결제 금액을 합산한다.
5. `실제 결제 금액 - 부담해야 할 금액`으로 회원별 잔액을 계산한다.
6. 일반 지출과 여러 회차의 잔액을 회원별로 합산한다.
7. 받을 금액이 있는 회원과 보낼 금액이 있는 회원을 분리한다.
8. 큰 금액부터 서로 연결하여 송금 횟수를 줄인 정산 계획을 만든다.
9. 최종 결과를 `보내는 회원 → 받는 회원 → 금액` 형식으로 저장한다.

기존 정산 결과가 있으면 새 계산 결과로 방 단위 전체 교체한다.

### 11.3 정산 현황

정산 현황 화면은 방 참여자별 상태와 개인 정산 내역을 제공한다.

- 참여자를 선택하면 해당 회원이 줄 금액과 받을 금액을 표시한다.
- 정산할 내용이 없으면 해당 없음으로 표시한다.
- 각 송금 관계의 정산 완료 여부를 표시한다.
- 다른 회원의 정산 내역은 조회만 가능하다.
- 본인의 정산 화면에서만 완료 체크를 변경할 수 있다.
- 완료 체크 변경은 즉시 CSV에 저장되고 완료 항목의 화면 색상을 갱신한다.

---

## 12. 데이터 저장 구조

시스템은 별도 데이터베이스 없이 다음 CSV 파일을 사용한다.

| 파일 | 저장 내용 |
|---|---|
| `members.csv` | 이름, 닉네임, 아이디, 비밀번호, 이메일 |
| `rooms.csv` | 방 코드, 이름, 종류, 방장, 참여자 목록 |
| `availability.csv` | 단체 약속의 회원별 날짜·시작 시간·종료 시간 |
| `travel_dates.csv` | 단체 여행의 회원별 시작일·종료일 |
| `places.csv` | 방별 장소 후보 |
| `place_votes.csv` | 방·장소·회원별 투표 |
| `room_final.csv` | 방별 최종 날짜·시간·장소 |
| `meeting_rounds.csv` | 방별 모임 회차 |
| `round_participants.csv` | 회차별 참여 확정 회원 |
| `expenses.csv` | 일반 및 회차별 지출 |
| `settlements.csv` | 송금 관계, 금액, 완료 여부 |

CSV 값에 쉼표나 큰따옴표가 포함된 경우에도 읽고 쓸 수 있도록 공통 CSV 변환 기능을 사용한다.

---

## 13. 현재 구현 범위 및 제약

- 데스크톱 Java Swing 애플리케이션이며 콘솔 시스템이나 웹 시스템이 아니다.
- 데이터는 로컬 CSV 파일에 평문으로 저장된다.
- 별도의 관리자 모드, 서버, 데이터베이스, 네트워크 동기화 기능은 없다.
- 비밀번호 암호화와 비밀번호 찾기 기능은 구현되어 있지 않다.
- 회차 참여는 확정 후 취소할 수 없다.
- 모임방 자체 삭제 기능은 없으며 사용자는 방 나가기만 할 수 있다.
- 최종 결정 권한과 참여자 강퇴 권한은 방장에게만 제공된다.
